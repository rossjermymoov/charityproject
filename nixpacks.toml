[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["cd packages/web && npm ci"]

[phases.build]
cmds = ["cd packages/web && npx prisma generate && npx next build"]

[start]
cmd = "cd packages/web && bash scripts/start.sh"
